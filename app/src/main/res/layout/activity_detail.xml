<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context="com.alezniki.notepad.activities.DetailActivity">

    <include layout="@layout/toolbar" />

    <!--Detail note title-->
    <EditText
        android:id="@+id/et_detail_note_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="@string/edit_title_detail"
        android:padding="@dimen/padding_text"
        android:textAlignment="textStart"
        android:textSize="@dimen/text_size_note_title"
        android:textStyle="bold" />

    <!--Detail note text-->
    <EditText
        android:id="@+id/et_detail_note_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="@string/edit_note_detail"
        android:padding="@dimen/padding_text"
        android:textAlignment="textStart"
        android:textSize="@dimen/text_size_note_text" />
</LinearLayout>
